{
  "name": "fastapi-personal-dashboard",
  "private": true,
  "description": "Full stack: Postgres + Redis + Backend + React — one command",
  "scripts": {
    "start": "docker compose -f docker-compose.full.yml up --build",
    "up": "docker compose -f docker-compose.full.yml up --build",
    "dev": "docker compose -f docker-compose.dev.yml up",
    "dev:build": "docker compose -f docker-compose.dev.yml up --build",
    "down": "docker compose -f docker-compose.full.yml down",
    "down:dev": "docker compose -f docker-compose.dev.yml down",
    "stop": "docker compose -f docker-compose.full.yml down"
  }
}
